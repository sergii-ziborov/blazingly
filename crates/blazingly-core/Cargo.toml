[package]
name = "blazingly-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Runtime-neutral operation model, HTTP projections, and typed response types for Blazingly"
license.workspace = true
repository.workspace = true
readme.workspace = true
keywords.workspace = true
categories.workspace = true
publish.workspace = true

[dependencies]
blazingly-contract.workspace = true
blazingly-json.workspace = true
memchr.workspace = true
serde.workspace = true

[dev-dependencies]
# The streaming multipart reader is pull-based, so its tests have to drive a
# future. Test-only; the crate itself still ships no runtime.
futures-lite.workspace = true

[lints]
workspace = true

