[package]
name = "blazingly-wire-standalone"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Standard-library smoke server that exercises the blazingly-wire HTTP/1 codec"
license.workspace = true
repository.workspace = true
readme.workspace = true
# Test harness binary, not part of the published surface. Unlike the rest of the
# workspace this stays `false` after the docs/stability.md release gate passes.
publish = false

[dependencies]
blazingly-wire.workspace = true
httpdate.workspace = true

[lints]
workspace = true
